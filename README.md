# 🌿 Plant Disease Detection using Vision Transformer

This project implements a Vision Transformer (ViT-B/16) model for plant disease detection using the PlantDoc dataset.

## Features
- Vision Transformer (ViT)
- Real-world PlantDoc dataset
- CPU-based training
- Streamlit web application
- Confidence-based predictions

## Tech Stack
- Python
- PyTorch
- Vision Transformer (ViT)
- Streamlit

## How to Run
```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python -m streamlit run app.py

Dataset

PlantDoc dataset (not included due to size).

Output

Disease prediction

Confidence score


---

## ✅ STEP 5 — Initialize Git Repository

In **PowerShell (project root)**:
```powershell
git init
